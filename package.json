{
  "name": "jekson",
  "description": "A JSON parser using a projection",
  "author": "David Wood <david.p.wood@gmail.com>",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "main": "build/jekson.js",
  "scripts": {
    "build": "tsc -p src",
    "test": "mocha --recursive build",
    "postinstall": "cd src && typings install"
  },
  "dependencies": {},
  "devDependencies": {
    "mocha": "^3.0.2",
    "typescript": "^1.8.10",
    "typings": "^1.3.3"
  }
}
